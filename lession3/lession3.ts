function myPerson(name: string) {
    var greet;
    if(name === "Paulo Dybala") {
        greet = 'Helo ' + name;
    }
    else {
        greet = 'Hello strager';
    }
    console.log(greet);
}
myPerson("Paulo Dybala");